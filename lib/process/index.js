const Agent = require('./agent');
const { Duplicator, duplicator } = require('./duplicator');
const { Mixer, mixer } = require('./mixer');

module.exports = {
  Agent,
  Duplicator,
  duplicator,
  Mixer,
  mixer,
}
